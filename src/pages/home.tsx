import * as React from 'react';
import styled from 'styled-components';

const Wrapper = styled.div`
`


const Home = () => {
    return (
        <div>
            Home
        </div>
    );
};

export default Home;